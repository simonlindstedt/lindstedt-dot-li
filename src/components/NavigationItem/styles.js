import styled from "styled-components";

export const StyledNavButton = styled.button`
  position: absolute;
  right: calc(32px / 2 + 8px);
  font-weight: 700;
  color: white;
  margin: 8px;
  cursor: pointer;
  width: 32px;
  height: 32px;
  border-radius: 50%;
  border: none;
  box-shadow: 2px 2px 10px 0px rgba(0, 0, 0, 0.2);
  transition: transform 0.2s 0s ease-in-out;
`;
